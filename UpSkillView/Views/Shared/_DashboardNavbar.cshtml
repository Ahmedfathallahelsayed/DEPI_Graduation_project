<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top">
    <div class="container-fluid px-4">
        <button class="btn btn-link text-dark d-lg-none me-2" id="sidebarToggle" aria-label="Toggle navigation">
            <i class="bi bi-list fs-4"></i>
        </button>
        <a class="navbar-brand m-0" asp-controller="Home" asp-action="Index">
            <i class="bi bi-book-half me-2 text-primary"></i>UpSkill LMS
        </a>
        
        <div class="ms-auto d-flex align-items-center gap-3">
            <div class="dropdown">
                <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="https://ui-avatars.com/api/?name=@(User.Identity?.Name ?? "User")&background=2563EB&color=fff" alt="" width="32" height="32" class="rounded-circle me-2">
                    <span class="text-dark d-none d-sm-inline">@(User.Identity?.Name ?? "User")</span>
                </a>
                <ul class="dropdown-menu dropdown-menu-end text-small shadow-sm" aria-labelledby="dropdownUser1">
                    @if(User.IsInRole("Student"))
                    {
                        <li><a class="dropdown-item" asp-controller="Student" asp-action="Dashboard">Dashboard</a></li>
                    }
                    else if (User.IsInRole("Instructor"))
                    {
                        <li><a class="dropdown-item" asp-controller="Instructor" asp-action="Dashboard">Dashboard</a></li>
                    }
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <form asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
                            <button type="submit" class="dropdown-item text-danger">Sign out</button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
